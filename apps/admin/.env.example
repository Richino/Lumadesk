# Supabase — same project as the storefront.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only. Never expose to the client. Used for admin aggregate reads
# and privileged writes behind the requireAdmin() gate.
SUPABASE_SERVICE_ROLE_KEY=
